---
paper: 2103.00020
---
CLIP 的批内对比学习构造：取一个 batch 的 N 个（图像，文本）对，编码后计算 N×N 的余弦相似度矩阵。对角线上的 N 个配对是数据集里真实匹配的图文对，即**正样本**；矩阵其余 N²−N 个位置是同一 batch 内错配的图文组合，全部作为**负样本**【来源 1 · p.3】。

预测任务是对称的：给定图像在 N 个文本中找正确的（按行 softmax），给定文本在 N 个图像中找正确的（按列 softmax），共享同一个相似度矩阵，优化对称的 InfoNCE 目标【来源 1 · p.3】。
