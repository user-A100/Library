# `.agents/`

Vault-local agent assets. Create Vault seeds this tree (no overwrite).

```text
.agents/
└── skills/<id>/SKILL.md   # Composer `$` skills; see skills/README.md
```

Add a skill at `.agents/skills/<id>/SKILL.md`. Do not store model API keys here (BYOA).
